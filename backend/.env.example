# E Lost & Found - Fill MONGODB_URI with your Atlas connection string
# Never commit .env to git

# MongoDB Atlas - replace USER, PASSWORD, and cluster host with your values (Atlas > Connect > Connect your application)
MONGODB_URI= mongodb+srv://ranjanshushant5_db_user:4*Um3!0D@cluster0.gnyqmsh.mongodb.net/elostfound?retryWrites=true&w=majority 

# JWT - random secret (change in production if you want)
JWT_SECRET=9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# OTP - how long OTP codes are valid (minutes)
OTP_EXPIRE_MINUTES=10

# AWS - leave empty; fill when you set up S3 and SES
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
S3_BUCKET=elost-found-uploads
SES_FROM_EMAIL=

# SMTP (optional - for OTP/email without AWS; e.g. Gmail app password)
# If SMTP/SES are not configured, OTP endpoints return "dev_otp" in API response for local testing.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=mydesktop2818@gmail.com
SMTP_PASSWORD=jkfadpzeezpcemxs
SMTP_FROM=E Lost & Found <mydesktop2818@gmail.com>

# CORS - frontend origins (local dev)
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,http://localhost:8000

# Admin - put your email here to get admin role after OTP login
ADMIN_EMAILS=admin123@gmail.com
ADMIN_PANEL_PASSWORD=$Admin@12345$
# API base URL for frontend (local dev)
API_BASE_URL=http://localhost:5500