/**
 * main.js - Init and nav (optional). Homepage stats are loaded in index.html inline script.
 */

// Optional: set API base URL from a global (e.g. set by server or build)
// window.API_BASE_URL = 'https://your-api.com';
